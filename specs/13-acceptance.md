# Acceptance
Required E2E path:
source -> parse -> extract -> canonicalize -> fuse -> page -> indexes/graph -> search -> Explore -> Context Bundle.

The implementation must remain useful with partial source materials and must explicitly report missing context.
