# Incremental Build and Quality
Each source has fingerprint.
Each fragment records source identity.
Changed sources invalidate only affected canonical contexts.
Quality checks include duplicate paths, alias collisions, broken confirmed references, missing evidence, invalid coverage and schema consistency.
